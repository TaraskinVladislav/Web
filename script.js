// Объявление переменных
var admin;
var name;

// Запись строки "Джон" в переменную name
name = "Джон";

// Копирование значения из переменной name в переменную admin
admin = name;

// Вывод значения переменной admin с помощью функции alert
alert(admin); // Выведет "Джон"
